# Scores Microservice

This microservice handles the scoring logic for offers.

## Endpoints
- `GET /ping`: Health check.
- `POST /scores`: Create a score.
- `GET /scores/{offer_id}`: Get score by offer ID.
- `GET /scores/count`: Count scores.
- `POST /scores/reset`: Reset data.
