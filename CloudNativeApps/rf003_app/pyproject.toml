[project]
name = "rf003-app"
version = "0.1.0"
description = "Orchestrator for RF-003"
requires-python = "~3.11"
dependencies = [
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
